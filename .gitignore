.venv/
__pycache__/
*.pyc
.env
.DS_Store
*.log
metoy-agent-code.zip

# Local-only source materials. The deployed app uses the compact JSONL index in data/.
新的设计/
从0构建大模型_大作业.pdf

# Keep Streamlit deployment config, but ignore local secrets.
.streamlit/secrets.toml
